import React from 'react';
import { Helmet } from 'react-helmet';

const Error = () => {
    return (
        <div>
            <Helmet>
                <title>Error</title>
            </Helmet>
        </div>
    );
};

export default Error;